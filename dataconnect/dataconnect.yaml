specVersion: "v1beta"
serviceId: "kurs"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kurs-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
